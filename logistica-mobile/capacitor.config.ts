import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.logisticafernaguez.mobile',
  appName: 'Logistica Fernaguez Mobile',
  webDir: 'dist'
};

export default config;
